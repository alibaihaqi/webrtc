apiVersion: v1
kind: Service
metadata:
  name: webrtc-redis
  namespace: webrtc
  labels:
    app: webrtc-redis
spec:
  type: ClusterIP
  selector:
    app: webrtc-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
      protocol: TCP
