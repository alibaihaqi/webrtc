apiVersion: v1
kind: Service
metadata:
  name: webrtc-signaling
  namespace: webrtc
  labels:
    app: webrtc-signaling
spec:
  type: ClusterIP
  selector:
    app: webrtc-signaling
  ports:
    - name: ws
      port: 3001
      targetPort: 3001
      protocol: TCP
